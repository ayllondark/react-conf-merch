import React from 'react';
import ReactDOM from 'react-dom';

import App from './routes/App.jsx';

ReactDOM.render(<App />, document.getElementById('app')); // <App  primer valor que le pasamos a reactDOM hacemos referencia al componente que tenemos
// y en segunda vamos hacer referencia a nuestra aplicacion con el documentbyid()  que crearemos en el index HTML publico
